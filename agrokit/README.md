# AgroKit

Python-фреймворк Starter Kit для AgroTech Robotics Challenge.

```bash
pip install -e .
```

Требует `unitree_sdk2_python` и CycloneDDS 0.10.2 — устанавливаются в Docker-образе
или вручную по [инструкции Unitree](https://github.com/unitreerobotics/unitree_sdk2_python).
